# Alan Kay — media from mail

*Guest hub:* [`../../README.md`](../../README.md)

Vetted attachment list for invitation browsing.

| File | Purpose |
|------|---------|
| [`REVIEW.yml`](REVIEW.yml) | Triage — `decided` vs `needs_don` |
| [`APPROVAL-TABLE.md`](APPROVAL-TABLE.md) | **Don checklist** — links + approve/reject boxes |
| [`MANIFEST.yml`](MANIFEST.yml) | Approved artifacts only |

Workflow: set `review: approved` on rows in `needs_don` → update `MANIFEST.yml`.
