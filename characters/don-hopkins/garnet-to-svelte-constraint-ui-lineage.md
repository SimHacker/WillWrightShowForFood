# Garnet → OpenLaszlo → Svelte — declare constraints, let the system keep them true

The **reactive UI braid** — same idea reinvented each GUI generation:

| Era | Tech | Idea |
|-----|------|------|
| ~1992 CMU | **Garnet** (Brad Myers) | KR constraints in Common Lisp — Don in Opal layer |
| 2000s | **OpenLaszlo** (Henry Minsky) | Declarative XML + real constraint system on the web |
| 2000s | **Zope → TurboGears** | Object publishing → lean Python (NutritionQuest decade+) |
| now | **SvelteKit / MOOLLM** | `$state` / `$derived`; YAML objects declare relationships |

Don rode every generation — which is why MOOLLM's reactive YAML feels obvious to him.

→ [`career/lineage.yml`](career/lineage.yml) (`tools_lineage`) · Henry → Leela band back together
