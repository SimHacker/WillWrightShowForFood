-- Ebike Safari — extensions (run as superuser if needed)
CREATE EXTENSION IF NOT EXISTS postgis;

-- Optional for OSM topology / graph analysis later:
-- CREATE EXTENSION IF NOT EXISTS postgis_topology;
