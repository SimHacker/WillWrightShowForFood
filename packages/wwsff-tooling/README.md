# @wwsff/tooling

Shared dev utilities for the WillWrightShowForFood monorepo.

Today: root `scripts/` hold verify tasks invoked via `pnpm run verify` from the repo root.

Future: sync helpers (`wwsff-sync`), schema validators, show-harvest tooling.
