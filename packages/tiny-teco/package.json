{
	"name": "@wwsff/tiny-teco",
	"version": "0.1.0",
	"private": true,
	"description": "TinyTeco — a TECO that can grow. ITS subset first (Minsky 1981 UTM mail).",
	"type": "module",
	"exports": {
		".": "./dist/index.js"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "tsc -p tsconfig.json && node --test dist/*.test.js",
		"cli": "tsc -p tsconfig.json && node dist/cli.js"
	},
	"devDependencies": {
		"@types/node": "^20.17.0",
		"typescript": "^5.8.0"
	}
}
