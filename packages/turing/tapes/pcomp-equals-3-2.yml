# Built from the PCOMP disk's own documentation for this machine.
# Generated by packages/turing/scripts/pcomp.py convert.

id: pcomp-equals-3-2
machine: pcomp-equals
blank: "0"
cells: "111011"
origin: 0
head: 3
why: "The disk's bug: when the left number is one more than the right, state 7 erases the extra 1 and steps left onto a 0, so 3 vs 2 reads as equal"
expect:
  halt: true
  scanned: "0"
