# Guest invitation — maps to invitation-workflow.yml (legacy alias)

see: invitation-workflow.yml
portrayal: portrayal-standards.yml

# Prefer invitation.status in CHARACTER.yml and frontmatter in invitation.md

invitation_status:
  deprecated_alias_for: invitation-workflow.status
  default: draft

consent:
  enum: [not_yet_asked, granted, declined]
  default: not_yet_asked

fields:
  show_seed: { type: path, example: "repo-shows/will-wright/" }
