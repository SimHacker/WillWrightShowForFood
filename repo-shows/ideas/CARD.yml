# CARD — repo-shows/ideas/

room:
  id: ideas
  name: "Show ideas (dissolving room — bits moved to /bits/, tracks to /process/tracks/)"
  type: [creative_inventory]
  parent: ../

registry: INDEX.yml

moved_out:
  bits: ../../bits/          # traditions, gags, themes, swag (kind-prefixed)
  tracks: ../../process/tracks/

remaining:
  - shows/                   # will-wright-premiere pack → Phase 2
  - "*.yml idea seeds → flat repo-shows/ dirs in Phase 2"

methods:
  LIST-ALL: INDEX.yml
  ADD: "New bits go to ../../bits/; new show ideas get flat repo-shows/ dirs"

relationships:
  show_proposals: ../INDEX.yml
  will_episodes: ../will-wright-premiere/episodes.yml
  will_segments: ../will-wright-premiere/SEGMENTS.yml
  live_rigs: ../../process/performance-space/
  forward_reactor: ../../process/crazy-idea-jam.yml

see_also:
  - README.md
  - GLANCE.yml
  - shows/will-wright-premiere/README.md
